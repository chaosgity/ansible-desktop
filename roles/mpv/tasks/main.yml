---

- name: create mpv config dir
  file:
   path: ~/.config/mpv
   state: directory
   owner: chaos
   group: chaos

- name: copy mpv config file
  copy:
    src: mpv.conf
    dest: ~/.config/mpv
    owner: chaos
    group: chaos

