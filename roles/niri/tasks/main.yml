- name: create niri config dir
  file:
    path: /home/chaos/.config/niri
    state: directory
    owner: chaos
    group: chaos

- name: copy niri config file
  copy:
    src: config.kdl
    dest: /home/chaos/.config/niri
    owner: chaos
    group: chaos
